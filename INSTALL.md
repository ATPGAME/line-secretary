# ติดตั้งแบบ Wizard — คู่มือสำหรับ Claude

> **ไฟล์นี้เขียนถึง Claude ที่กำลังช่วยผู้ใช้ติดตั้ง** ผู้ใช้เป็นมือใหม่ ไม่เคยเขียนโค้ด อาจกำลังอัดคลิปอยู่
> เป้าหมาย: ใช้งานได้จริงภายใน 2 ชั่วโมง โดยผู้ใช้ไม่ต้องพิมพ์คำสั่งเองสักบรรทัด

## กฎเหล็ก 7 ข้อ — ห้ามฝ่าฝืน

1. **ทำทีละขั้นเท่านั้น** ห้ามข้าม ห้ามทำสองขั้นในข้อความเดียว ห้ามถามหลายค่าพร้อมกัน
2. **ขึ้นต้นทุกข้อความด้วยแถบความคืบหน้า** เช่น `[ขั้น 3/10] ✅✅⬜⬜⬜⬜⬜⬜⬜⬜ — สมัคร OpenRouter`
3. **จบทุกขั้นด้วยการตรวจ** ต้องรัน `node scripts/check.mjs ...` แล้วเห็น ✅ ก่อนถึงจะขึ้นขั้นถัดไป ห้ามเชื่อคำบอกเล่าของผู้ใช้ว่า "ทำแล้ว"
4. **บอกให้กดอะไร ต้องบอกว่าจะเห็นอะไร** เช่น "กด Issue แล้วจะมีตัวอักษรยาว ๆ ขึ้นมา ก๊อปมาวางในแชทนี้"
5. **ห้ามโชว์ค่าลับซ้ำ** ผู้ใช้วาง token มาแล้ว เขียนลง `.env.local` อย่างเดียว ห้าม echo ห้ามพิมพ์ทวนในแชท (เขาอาจกำลังแชร์จอ/อัดคลิป)
6. **error ต้องแปลเป็นภาษาคน** ห้ามวาง stack trace ให้ผู้ใช้ดู บอกแค่ว่าอะไรพัง + ต้องทำอะไร
7. **อัปเดตไฟล์ `.setup-progress.md` ทุกครั้งที่จบขั้น** เพื่อว่าถ้าปิดแชทแล้วเปิดใหม่ จะทำต่อจากเดิมได้

## เริ่มต้นทุกครั้ง

อ่าน `.setup-progress.md` ก่อน (ถ้ามี) แล้วบอกผู้ใช้ว่า "ครั้งที่แล้วทำถึงขั้น X นะครับ ทำต่อเลย"
ถ้าไม่มีไฟล์นี้ = เริ่มใหม่จากขั้น 1 และสร้างไฟล์ขึ้นมา รูปแบบ:

```markdown
# ความคืบหน้าการติดตั้ง
- [x] 1. เตรียมโปรเจค
- [ ] 2. LINE OA
...
โดเมน Vercel: (ยังไม่มี)
```

---

## ขั้น 1/10 — เตรียมโปรเจค

```bash
npm install
cp .env.example .env.local
```
สุ่มรหัส dashboard ให้เลย ไม่ต้องถามผู้ใช้ แล้วเขียนลง `.env.local` เป็น `DASHBOARD_KEY`:
```bash
node -e "console.log(require('crypto').randomBytes(12).toString('hex'))"
```
บอกผู้ใช้ว่า "เตรียมของเรียบร้อย ต่อไปเราจะไปสร้าง LINE OA กัน ใช้เวลาประมาณ 10 นาที"

## ขั้น 2/10 — สร้าง LINE OA

**เริ่มที่ https://manager.line.biz** (LINE Official Account Manager) — ที่นี่คือที่สร้าง OA และตั้งค่าการตอบกลับ

> **ทำไมต้องเริ่มที่นี่:** พอสร้าง OA ที่ manager แล้วกดเปิด Messaging API มันจะ **สมัคร developer account + สร้าง channel ให้อัตโนมัติ**
> ผู้ใช้ไม่ต้องไปสมัคร developers.line.biz แยกเลย (ถ้าเริ่มจากฝั่ง developers ก่อน จะต้องสร้าง provider + channel เองแล้วสับสนว่า OA ตัวไหนคู่กับ channel ไหน)
> ถ้าผู้ใช้ถามว่า "ต้องสมัคร developer ก่อนไหม" ตอบได้เลยว่าไม่ต้อง

พูดกับผู้ใช้ทีละข้อ:
1. เปิด https://manager.line.biz แล้วเข้าสู่ระบบด้วยบัญชี LINE ที่ใช้ในมือถือ
2. กด **สร้างบัญชีใหม่** (Create account) → กรอกชื่อบัญชี (ชื่อนี้คนจะเห็นใน LINE) ประเภทธุรกิจ → ยืนยัน
3. เข้าไปในบัญชีที่เพิ่งสร้าง → **การตั้งค่า** (Settings มุมขวาบน) → **การตอบกลับ** (Response settings)
   - **ปิด** ข้อความตอบกลับอัตโนมัติ
   - **ปิด** ข้อความต้อนรับ

อธิบายสั้น ๆ ว่าทำไมต้องปิด: "ถ้าไม่ปิด LINE จะตอบข้อความสำเร็จรูปแทนบอทเรา แล้วบอทจะเงียบทั้ง ๆ ที่โค้ดถูก — เป็นจุดที่คนพลาดเยอะที่สุด"

⚠️ **ตรงนี้จะเห็นสวิตช์ Webhook ด้วย แต่ยังเปิดไม่ได้ตอนนี้** — LINE ให้เปิดได้ต่อเมื่อใส่ Webhook URL แล้ว ซึ่งเราจะมี URL ตอนขั้น 7 (หลัง deploy)
ถ้าผู้ใช้ถามว่าทำไมกดไม่ได้ / กดแล้วเด้งกลับ ให้บอกว่า "ปกติครับ เดี๋ยวขั้น 8 เรากลับมาเปิด" — อย่าให้เขาเสียเวลางมตรงนี้

ถามยืนยันว่าปิดครบ 2 อันแล้วค่อยไปต่อ

## ขั้น 3/10 — เปิด Messaging API แล้วเอากุญแจมา

ยังอยู่ที่ manager.line.biz:
1. **การตั้งค่า** → **Messaging API** → กด **ใช้ Messaging API**
2. เลือก provider (ถ้ายังไม่มี ให้สร้างใหม่ ใส่ชื่ออะไรก็ได้ เช่นชื่อร้าน) → ยืนยัน
3. จะได้ **Channel secret** ขึ้นมาในหน้านี้ → ขอค่านี้จากผู้ใช้
   → เขียนลง `.env.local` เป็น `LINE_CHANNEL_SECRET`

จากนั้นไปเอา token ที่ https://developers.line.biz (ล็อกอินบัญชีเดียวกัน จะเห็น channel ที่เพิ่งสร้าง):
4. เลือก provider → เลือก channel → แท็บ **Messaging API** → เลื่อนล่างสุด **Channel access token (long-lived)** → กด **Issue**
   → ตัวอักษรยาวมาก ขอมาวาง → เขียนลง `.env.local` เป็น `LINE_CHANNEL_ACCESS_TOKEN`

**ตรวจก่อนไปต่อ:**
```bash
node scripts/check.mjs line
```
ต้องขึ้น ✅ พร้อมชื่อ OA — บอกชื่อ OA ที่ขึ้นมาให้ผู้ใช้ฟังด้วย เขาจะได้มั่นใจว่าต่อติดจริง
(ถ้าขึ้น 401 = ก๊อป token มาไม่ครบ หรือกด Issue ใหม่ทับของเก่าไปแล้ว)

## ขั้น 4/10 — เปิดให้บอทเข้ากลุ่มได้

ที่ https://developers.line.biz (หน้าเดิมกับที่เพิ่งเอา token):
แท็บ **Messaging API** → หา **Allow bot to join group chats** → กด **Edit** → เปิด

ถ้าไม่เปิด จะเชิญบอทเข้ากลุ่มไม่ได้เลย (ฟีเจอร์เฝ้ากลุ่ม/สรุปกลุ่มจะใช้ไม่ได้ทั้งหมด)

ถามยืนยันว่าเปิดแล้วค่อยไปต่อ

## ขั้น 5/10 — สมัคร OpenRouter (สมองของบอท)

1. เปิด https://openrouter.ai → **Sign in with Google**
2. เมนู **Credits** → เติม $5 (ประมาณ 170 บาท ใช้ได้เป็นเดือน)
3. เมนู **Keys** → **Create key** → ตั้งชื่ออะไรก็ได้ → ก๊อปมาวางในแชท (ขึ้นให้ดูครั้งเดียว)
   → เขียนลง `.env.local` เป็น `OPENROUTER_API_KEY`

**ตรวจก่อนไปต่อ:**
```bash
node scripts/check.mjs openrouter
```
ถ้าบอกว่าเครดิตหมด ให้กลับไปเติมเงินก่อน

## ขั้น 6/10 — เอาขึ้นเว็บจริง (Vercel)

1. ให้ผู้ใช้สมัคร https://vercel.com ด้วยปุ่ม **Continue with GitHub** (ถ้ายังไม่มีบัญชี GitHub ให้สมัคร github.com ก่อน)
2. รันให้เขา:
```bash
npx vercel@latest login
npx vercel@latest --prod --yes
```
ตอบคำถามที่ CLI ถามด้วยค่า default ได้หมด (กด Enter รัว ๆ)

3. deploy รอบแรกยังใช้ไม่ได้ — ปกติ เพราะยังไม่มีค่าอะไรเลย บอกผู้ใช้ไว้ก่อนเขาจะได้ไม่ตกใจ
   **จดโดเมนที่ได้ลง `.setup-progress.md`**

## ขั้น 7/10 — สร้างฐานข้อมูล Neon จากในหน้า Vercel

**ไม่ต้องสมัคร neon.tech แยก** — สร้างจากในโปรเจค Vercel ได้เลย แล้ว `DATABASE_URL` จะถูกใส่ให้อัตโนมัติ

บอกผู้ใช้:
1. เปิด https://vercel.com → เลือกโปรเจคที่เพิ่ง deploy → แท็บ **Storage**
2. กด **Create Database** → เลือก **Neon** (Serverless Postgres) → Continue
3. region เลือก **Singapore** (ใกล้ไทยสุด) → **Create**

จากนั้นรันให้เขา — ดึงค่าที่ Vercel สร้างให้ลงมาใส่ `.env.local` (ค่าอื่นไม่ถูกแตะ):
```bash
node scripts/push-env.mjs pull
```

ส่งค่าที่เหลือขึ้น Vercel แล้ว deploy ซ้ำ (อ่านจาก `.env.local` ส่งผ่าน stdin — ค่าไม่โผล่บนหน้าจอ):
```bash
node scripts/push-env.mjs vercel
npx vercel@latest --prod --yes
```

**สร้างตาราง + ตรวจ:**
```bash
node scripts/setup.mjs
node scripts/check.mjs db
node scripts/check.mjs vercel https://โดเมนที่ได้
```
ทั้ง 2 ตัวหลังต้องขึ้น ✅ ก่อนไปต่อ

> ห้ามใช้ `vercel env pull .env.local` ตรง ๆ เด็ดขาด — มันเขียนทับทั้งไฟล์ ค่า LINE/OpenRouter ที่ยังไม่ได้ push จะหายหมด
> ใช้ `node scripts/push-env.mjs pull` ที่เติมเฉพาะ `DATABASE_URL` แทน

## ขั้น 8/10 — ต่อ LINE เข้ากับเว็บ (อัตโนมัติ)

```bash
node scripts/setup.mjs https://โดเมนที่ได้
```
สคริปต์จะใส่ Webhook URL ให้เองและยิงทดสอบ ต้องเห็น `✅ LINE ยิงทดสอบผ่าน`

**ตอนนี้ค่อยกลับไปเปิดสวิตช์ Webhook ที่ค้างไว้จากขั้น 2** (พอมี URL แล้ว LINE ถึงจะยอมให้เปิด):
- https://manager.line.biz → **การตั้งค่า → การตอบกลับ** → **เปิด Webhook**
- (หรือที่ developers.line.biz แท็บ Messaging API → **Use webhook** → เปิด — ตัวเดียวกัน)

⚠️ ถ้าข้ามข้อนี้ บอทจะเงียบสนิททั้งที่ทุกอย่างถูกหมด เพราะ LINE ไม่ส่งข้อความมาให้เลย

แล้วบอกผู้ใช้: "เปิดแท็บ Messaging API แล้วสแกน **QR code** ด้วยมือถือ กดเพิ่มเพื่อน แล้วทักบอทว่า **ไอดี** ครับ"
→ บอทจะตอบ userId กลับมา ให้ผู้ใช้ก๊อปมาวางในแชท
→ เขียนลง `.env.local` เป็น `OWNER_USER_ID` แล้ว:
```bash
node scripts/push-env.mjs vercel && npx vercel@latest --prod --yes
```

**ถึงตรงนี้เลขาใช้งานได้แล้ว** — ให้ผู้ใช้ลองทันที 3 อย่าง แล้วรอฟังผลจากเขา:
- พิมพ์ `จดไว้ รหัส wifi บ้าน 12345678`
- พิมพ์ `รหัส wifi อะไรนะ`
- ส่งรูปสลิปโอนเงินอะไรก็ได้ในมือถือ

ถ้าผ่านทั้ง 3 ให้ชมเขาหน่อย เขาเพิ่งสร้างบอทตัวแรกเสร็จ

## ขั้น 9/10 — ให้มันรายงานเอง (เลือก 1 ใน 2 แบบ)

**ถามผู้ใช้ก่อนว่าจะเอาแบบไหน** อย่าเลือกให้เขาเอง:

| | แบบฟรี (Vercel cron) | แบบเสียเงิน (Railway $5/เดือน) |
|---|---|---|
| สรุปกลุ่ม | วันละ 1 ครั้ง ~8 โมงเช้า (คลาดได้ถึง 1 ชม.) | เช้า-เย็น หรือกี่รอบก็ได้ ตรงเวลา |
| เตือนลูกค้าถามค้าง | ได้แค่ตอนรอบวันละครั้ง = แทบไม่มีประโยชน์ | ทุก 10 นาที ใช้ได้จริง |
| ต้องทำอะไรเพิ่ม | ไม่ต้องทำอะไรเลย มีให้แล้วใน `vercel.json` | ติดตั้ง Railway |

พูดกับผู้ใช้ประมาณนี้: "ถ้าแค่อยากได้สรุปตอนเช้า แบบฟรีพอครับ แต่ถ้าทำธุรกิจแล้วอยากรู้ทันทีตอนลูกค้าถามค้าง ต้องแบบ $5"

### ถ้าเลือกแบบฟรี
สุ่ม `CRON_SECRET` ให้ (กัน endpoint ถูกยิงมั่ว) แล้วส่งขึ้น Vercel + deploy:
```bash
node -e "console.log('CRON_SECRET='+require('crypto').randomBytes(16).toString('hex'))" >> .env.local
node scripts/push-env.mjs vercel
npx vercel@latest --prod --yes
```
ทดสอบว่าทำงานจริงโดยไม่ต้องรอถึงพรุ่งนี้ — เปิด `https://โดเมน/api/cron?key=DASHBOARD_KEY` ต้องได้ `{"ok":true,...}`
บอกผู้ใช้ว่า "ของจริงจะยิงเองทุกเช้าประมาณ 8 โมงครับ"

### ถ้าเลือก Railway
ทำผ่าน CLI ได้ทั้งหมด **ไม่ต้องกดอะไรในเว็บ Railway เลย** (Start Command อยู่ใน `railway.json` ของโปรเจคแล้ว)

1. ให้ผู้ใช้สมัคร https://railway.app → Login with GitHub → ผูกบัตร (Hobby $5/เดือน)
2. ล็อกอิน — คำสั่งนี้จะเปิดเบราว์เซอร์ให้ผู้ใช้กดยืนยัน 1 ครั้ง (เป็นขั้นเดียวที่ CLI ทำแทนไม่ได้):
```bash
npx @railway/cli login
```
บอกเขาว่า "เดี๋ยวเบราว์เซอร์จะเด้งขึ้นมา กด Authorize แล้วกลับมาที่นี่ครับ"

3. สร้างโปรเจค + deploy:
```bash
npx @railway/cli init -n line-secretary
npx @railway/cli up -y -d
```
(`-d` = ไม่ต้องค้างดู log · `railway.json` บอกให้รัน `node worker/index.js` และข้าม build ให้แล้ว)

4. ส่งค่าขึ้น:
```bash
node scripts/push-env.mjs railway
```

5. เช็คว่าขึ้นจริง:
```bash
npx @railway/cli logs
```
ต้องเห็น `worker started · ตื่นทุก 10 นาที` (ถ้ายังไม่ขึ้น รอ 1 นาทีแล้วดูใหม่ — Railway กำลัง build อยู่)

จากนั้นให้ผู้ใช้เชิญบอทเข้ากลุ่มทดสอบ → บอทจะแนะนำตัว → ลองพิมพ์ในกลุ่มว่า `เลขา สรุปให้หน่อย`

> ถ้า Railway ขึ้นมาแล้ว **ปิด cron ของ Vercel ทิ้ง** จะได้ไม่รายงานซ้ำสองรอบ: ลบ `vercel.json` แล้ว deploy Vercel ใหม่

---

## ขั้น 10/10 — ตั้งหน้าที่ให้บอท (ห้ามข้าม — นี่คือขั้นที่ทำให้บอทเป็น "ของเขา")

ตอนนี้บอทยังเป็นเลขาทั่วไป เหมือนของทุกคน ขั้นนี้คือทำให้มันรู้ว่าตัวเองทำงานให้ใคร ธุรกิจอะไร

**สัมภาษณ์ผู้ใช้ทีละคำถาม** (ถามทีละข้อจริง ๆ อย่ายิงรวด):

1. "ทำธุรกิจอะไรครับ หรือจะใช้ส่วนตัว?"
2. "อยากให้บอทเรียกคุณว่าอะไร แล้วอยากให้มันชื่ออะไร?"
3. "อยากให้มันช่วยเรื่องไหนมากที่สุด 2-3 อย่าง?" — ยกตัวอย่างให้เลือก:
   จดของที่ต้องจำ · เตือนงาน · จดรายจ่ายจากสลิป · สรุปกลุ่มลูกค้า · เตือนเมื่อลูกค้าบ่น · สรุปออเดอร์
4. "มีอะไรที่ห้ามทำเด็ดขาดไหม?" (เช่น คลินิกห้ามให้คำแนะนำทางการแพทย์ · ร้านค้าห้ามให้ส่วนลดเอง)

**แล้วลงมือทำให้เขา 3 อย่าง:**

1. เขียน `lib/persona.js` ใหม่จากคำตอบ (ในไฟล์มีตัวอย่าง 3 แบบให้ดูเป็นแนว) — เขียนเป็นภาษาไทย สั้น ตรง ไม่เกิน 6-7 บรรทัด
2. ถ้าเขาบอกหน้าที่ที่ยังไม่มีเครื่องมือรองรับ (เช่น "จดวันเกิดลูกค้า") → เพิ่ม tool ใน `lib/brain.js` ให้เลย
3. ตั้งค่ากลุ่มให้ตรงกับธุรกิจเขา:
```sql
update watched set report_hours = '{...}',   -- เวลาที่เขาสะดวกอ่าน
                   alert_words  = '{...}',   -- คำที่ธุรกิจเขาต้องรู้ทันที
                   sla_minutes  = 15,
                   track_orders = true       -- ถ้าเป็นกลุ่มรับออเดอร์
 where source_id = '...';
```

จากนั้น deploy แล้วให้เขาลองคุยกับบอทอีกรอบ — **เขาต้องรู้สึกว่ามันเปลี่ยนไป** ถ้ายังพูดเหมือนเดิม แปลว่า persona ยังจืดไป ให้เขียนใหม่ให้เฉพาะเจาะจงกว่าเดิม

```bash
node scripts/push-env.mjs vercel >/dev/null; npx vercel@latest --prod --yes
```
(ถ้าใช้ Railway ด้วย ต้อง `npx @railway/cli up` ซ้ำ เพราะ worker ใช้ persona เดียวกันตอนสรุปกลุ่ม)

## เช็คลิสต์ก่อนบอกว่า "เสร็จแล้ว"

รันตัวตรวจรวดเดียว: `node scripts/check.mjs all` ต้อง ✅ หมด แล้วให้ผู้ใช้ยืนยันว่าทำได้จริงทั้ง 5 ข้อ:

1. ทัก `จดไว้ ...` → บันทึกให้
2. ถามของที่จดไว้ → ตอบถูก
3. ส่งสลิป → บอกยอด
4. เปิดหน้าแรกของเว็บ → ✅ ทุกข้อ
5. เชิญเข้ากลุ่ม → บอทแนะนำตัว → สั่งสรุปกลุ่มได้
6. **บอทพูดจาแบบที่เขาสั่งไว้จริง ๆ** (เรียกชื่อถูก รู้ว่าตัวเองทำงานให้ธุรกิจอะไร)

ปิดท้าย: บอกสิ่งที่ปรับแต่งต่อได้ (เวลารายงาน · คำที่ให้เตือน · บุคลิกของบอท · เครื่องมือใหม่) และบอกว่าสั่งผ่านแชทนี้ได้เลยไม่ต้องแก้โค้ดเอง

## ถ้าติดขัด

- ผู้ใช้ติดขั้นเดิมเกิน 2 รอบ → ข้ามไปทำขั้นที่ทำได้ก่อน แล้ววนกลับมา อย่าปล่อยให้เขาค้างอยู่กับที่
- Verify ไม่ผ่าน / บอทเงียบ → 90% คือใส่ค่าบน Vercel แล้วลืม redeploy หรือลืมปิดข้อความอัตโนมัติในขั้น 4
- บอทตอบว่า "ระบบมีปัญหา" → เครดิต OpenRouter หมด หรือ `DATABASE_URL` ผิด — รัน `node scripts/check.mjs all` ดู
